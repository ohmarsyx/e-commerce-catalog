<%= form_with(model: product_group) do |form| %>
  <% if product_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_group.errors.count, "error") %> prohibited this product_group from being saved:</h2>

      <ul>
        <% product_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_group_name, style: "display: block" %>
    <%= form.text_field :product_group_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
